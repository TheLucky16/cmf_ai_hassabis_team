# Copy this file to `.env` and fill in your key.
#   cp .env.example .env
# Get a free key at https://aistudio.google.com/apikey
# The real .env is gitignored, so your key is never committed.
GEMINI_API_KEY=your-key-here
